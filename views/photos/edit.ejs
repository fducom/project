	
<% if (session.authenticated && session.User.admin) { %>	
	<form action="/photos/update/<%= photos.id %>" method="POST" class="form-edit">
		<h2> Редагувати photos </h2>

		<input type="hidden" value="<%= session.User.id %>" name="name" type="text" class="form-control"/>
		<input value="<%= photos.title %>" name="title" type="text" class="form-control"/>
		
		<textarea name="text" id="redactor"/><%= photos.text %></textarea>
		<input value="<%= photos.img %>" name="img" type="text" class="form-control"/>
				
		
	</div>
		<br>
		<input type="submit" value="Зберегти" class="btn btn-lg btn-primary btn-block"/>
		<input type="hidden" name="_csrf" value="<%= _csrf %>" />
	</form>

 <%} else { res.redirect('/session/new'); } %>